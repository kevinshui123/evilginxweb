import { ElementType } from 'react';
import { CNavLinkProps } from '@coreui/react/dist/esm/components/nav/CNavLink';
import { PolymorphicRefForwardingComponent } from '@coreui/react/dist/esm/helpers';
export interface CNavItemProps extends Omit<CNavLinkProps, 'component'> {
    /**
     * Component used for the root node. Either a string to use a HTML element or a component.
     *
     * @since 5.0.0
     */
    as?: ElementType;
}
export declare const CNavItem: PolymorphicRefForwardingComponent<'li', CNavItemProps>;
